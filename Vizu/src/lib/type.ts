export type ValidationPrpos= {
    visible: boolean,
        text: string,
        variant? : "error" | "success" | "info"
}

export type UrlProps = {
    glbUrl: string;
    usdzUrl?: string;
  };
  